# Baseline evidence — pre-migration snapshot

Reference snapshot of the repository state before the multi-profile migration.
All gate outputs below were captured on a clean tree.

- **Commit:** `6c2505607dc5fb65678c1cb776aa3b3eda92751e`
- **Package version:** `0.1.0`
- **Branch:** `feat/upstream-2.1.233-wave-0` (created from `main` @ `6c25056`)
- **Captured:** 2026-08-15
- **Toolchain:** node `v24.15.0`, npm `11.16.0` (no `packageManager` field in `package.json`)

## Gate outputs (literal)

### `npm ci`

Exit 0. One advisory warning (not a failure):

```
npm warn allow-scripts 1 package has install scripts not yet covered by allowScripts:
npm warn allow-scripts   workerd@1.20260722.1 (postinstall: node install.js)
```

### `npm run lint`

Exit 0. `eslint .` — no output (no findings).

### `npm run typecheck`

Exit 0. `tsc -p tsconfig.json --noEmit && tsc -p tsconfig.types.json --noEmit` — no output.

### `npm test`

Exit 0.

```
 Test Files  84 passed (84)
      Tests  2033 passed (2033)
```

- Test file count: **84** (`*.test.ts` under `test/`, recursive — matches the vitest run)
- Test case count: **2033**
- First-level directories under `test/`: `conformance`, `docs`, `drift`, `fixtures`, `governance`, `pack`, `property`, `runtime`, `security`, `support`, `types`, `validation`

### `npm run build`

Exit 0. `tsc -p tsconfig.json` — no output.

### `npm run test:coverage`

Exit 0.

```
=============================== Coverage summary ===============================
Statements   : 99.04% ( 2272/2294 )
Branches     : 98.8% ( 2152/2178 )
Functions    : 100% ( 270/270 )
Lines        : 99.69% ( 1988/1994 )
================================================================================
```

#### Configured thresholds vs effective values

Configured in `vitest.config.ts:16-21` (`test.coverage.thresholds`):

| Metric     | Configured threshold | Effective value | Headroom |
| ---------- | -------------------- | --------------- | -------- |
| statements | 98                   | 99.04           | +1.04    |
| branches   | 97                   | 98.80           | +1.80    |
| functions  | 100                  | 100.00          | 0.00     |
| lines      | 99                   | 99.69           | +0.69    |

The headroom on statements/branches means a refactor could reduce coverage by up
to ~1–1.8 points without tripping the configured gate. Any wave that touches
`src/` must therefore compare against the **effective** values above, not just
rely on the configured thresholds passing.

### `npm run pack:check`

Exit 0 (`publint` + `attw`):

```
node16 (from ESM): 🟢 (ESM)
bundler: 🟢
node10: (ignored) 💀 Resolution failed
node16 (from CJS): (ignored) ⚠️ ESM (dynamic import only)
```

### `npm run test:pack`

Exit 0. The three packed-consumer digests are identical:

```
node:    6b9609b29463c890544845dd94acf560206b6f8165538faafd8886750037d277
bun:     6b9609b29463c890544845dd94acf560206b6f8165538faafd8886750037d277
workerd: 6b9609b29463c890544845dd94acf560206b6f8165538faafd8886750037d277
```

### `npx vitest run test/drift`

Exit 0.

```
 Test Files  1 passed (1)
      Tests  9 passed (9)
```

### `npm run drift:check`

Exit 0 (source available — **not** `SOURCE_UNAVAILABLE`):

```
profile=claude-code-2.1.195-sdk-0.94.0 drift=none
```

The upstream comparison checkout `D:\git\opencode-anthropic-fix` **exists** on
this machine, which is why `drift:check` runs the full comparison instead of
exiting 2 with `SOURCE_UNAVAILABLE`. On machines without that checkout, exit 2
is the expected outcome.

## Golden fixture hashes

SHA-256 (lowercase hex) over the raw bytes of every file in
`test/fixtures/golden/`, captured at the commit above.

| file | sha256 |
| ---- | ------ |
| decision-context-hint-rejected.json | 6957d363e1e9512eb1a8d2c7170fa208b92e28460e7fb3b8576aa3814cdf4582 |
| manifest.json | a3f1b03feb85af6a1512450980c913d3e14c1471e9cfe8ad7fe394155c3dece0 |
| outgoing-canary-context-hint-off.json | af9fa1a299ba9b3cf493e1e5b2e0bb8b935e1089c2679ead615fe87c459bf3db |
| outgoing-foreground.json | 62748f01fcc20ae48f40dc4b628a094db5e06cd809d64d0c9163c0e69b0a98ea |

### Coherence against `manifest.json`

`test/fixtures/golden/manifest.json` (`sourceCommit
466d500084b59651798bf38bf24d21f3cb850db6`) lists hashes for the three fixture
payloads under its `fixtures` key. Item-by-item comparison against the values
computed above:

| file | manifest hash == computed hash |
| ---- | ------------------------------ |
| outgoing-foreground.json | MATCH |
| outgoing-canary-context-hint-off.json | MATCH |
| decision-context-hint-rejected.json | MATCH |

`manifest.json` itself is (correctly) not self-referenced in its own `fixtures`
map; its hash is recorded here so that any manifest edit is visible against this
baseline.

Conclusion: manifest and fixture files are coherent at the baseline commit.
